<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 日志目录：优先读 application.yml 中的 logging.file.path，未配置则相对 ${user.dir} -->
    <springProperty scope="context" name="LOG_DIR" source="logging.file.path" defaultValue="${user.dir}/log"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 按日期滚动的文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/sre-agent.log</file>

        <!-- 基于时间的滚动策略 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 每天一个文件，文件名包含日期 -->
            <fileNamePattern>${LOG_DIR}/sre-agent.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 只保留1天的日志 -->
            <maxHistory>1</maxHistory>
            <!-- 启动时删除过期日志 -->
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 请求专用日志（记录完整的请求/响应） -->
    <appender name="REQUEST" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/request.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/request.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>1</maxHistory>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 请求日志专用 Logger -->
    <logger name="REQUEST_LOG" level="INFO" additivity="false">
        <appender-ref ref="REQUEST"/>
    </logger>

    <!-- 应用日志 -->
    <logger name="com.yycome.sreagent" level="DEBUG"/>
    <logger name="com.yycome.sremate" level="DEBUG"/>
    <logger name="org.springframework.ai" level="DEBUG"/>

    <!-- 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
