## 1. 层级自适应实现

- [x] 1.1 删除硬编码的 `entityLevel` 对象
- [x] 1.2 实现 `calculateEntityLevels(entities, relations)` 函数，使用 BFS 从 Order 出发计算层级
- [x] 1.3 将计算出的层级应用到节点配置中

## 2. 节点视觉增强

- [x] 2.1 更新 `DOMAIN_COLOR` 配色方案，添加边框色和发光色属性
- [x] 2.2 根据计算出的层级为入口节点（Level 0）设置 `shape: 'box'`
- [x] 2.3 为节点配置多层阴影（shadow 配置）
- [x] 2.4 调整节点标签字体，实体名加粗、displayName 换行显示
- [x] 2.5 添加节点悬停效果配置（hover/highlight 状态）

## 3. 边视觉增强

- [x] 3.1 更新边颜色配置，使用目标节点的领域色
- [x] 3.2 增大箭头比例 `scaleFactor: 0.9`
- [x] 3.3 为边标签添加圆角背景样式
- [x] 3.4 添加边悬停效果配置

## 4. CSS 样式调整

- [x] 4.1 调整节点文字颜色，确保在渐变背景上可读
- [x] 4.2 优化侧边栏统计卡片样式，与新配色协调
- [x] 4.3 更新图例颜色，与新配色一致

## 5. 验证与测试

- [x] 5.1 启动应用，访问 `/ontology.html` 验证视觉效果
- [x] 5.2 验证 ContractInstance 显示在正确层级（Level 2）
- [x] 5.3 测试不同层级节点的形状区分
- [x] 5.4 测试节点和边的悬停效果
